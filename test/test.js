'use strict';
var assert = require('assert');
var pouchReplicateWebrtc = require('../');

describe('pouch-replicate-webrtc node module', function () {
  it('must have at least one test', function () {
    pouchReplicateWebrtc();
    assert(true, 'true');
  });
});
